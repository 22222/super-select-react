---
title: Material UI
---

import Example from "@site/src/examples/customization-material-ui";

# Material UI

This example shows one way to define a Material UI-flavored wrapper around `SuperSelect`.

```tsx
export function MaterialSuperSelect(props) {
    return (
        <SuperSelect
            {...props}
            customization={{
                classNamePrefix: "mui-super-select",
                modalSelectButton: {
                    component: MaterialSelectButton,
                    selectedContent: {
                        component: MaterialSelectedContent,
                    },
                },
                modal: {
                    component: MaterialModalDialog,
                    okButton: {
                        component: MaterialOkButton,
                    },
                },
                optionList: {
                    component: MaterialOptionListContainer,
                },
                optionListInput: {
                    component: MaterialOptionListInput,
                },
                searchInput: {
                    component: MaterialSearchInput,
                    placeholder: "Search options",
                },
                toggleButtonInput: {
                    component: MaterialToggleButtonInput,
                },
                selectInput: {
                    component: MaterialNativeSelect,
                },
            }}
        />
    );
}
```

See [website/src/components/MaterialSuperSelect.tsx](https://github.com/22222/super-select-react/blob/main/website/src/components/MaterialSuperSelect.tsx) for the full source code of this example:

<Example />

